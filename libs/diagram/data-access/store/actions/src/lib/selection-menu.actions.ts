import { createAction } from '@ngrx/store';

export const selectAllClick = createAction('[Selection Menu] Select All Click');

export const inverseSelectionClick = createAction('[Selection Menu] InverseSelectionClick');
